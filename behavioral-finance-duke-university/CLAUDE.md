# Project Context: Web Application

## Developer Profile
- **Author**: Nishant
- **GitHub**: nishant4671
- **Email**: nishantkr4671@gmail.com
- **Preferred Timezone**: Indian Standard Time (IST) (Asia/Kolkata). Ensure all logs, schedules, and timestamps default to IST.

## Personal Workflow Rules
- **Educational Mode**: When introducing a new design pattern, library, or complex mathematical/ML formula, add a brief 1-2 sentence comment explaining the concept to help the author learn.
- **Notion Dev Log Sync**: At the end of a session where files were changed, proactively ask the user if they would like you to write a bullet-point summary of the changes to their Notion Dev Log.

## Tech Stack & Architecture
- Framework: [e.g. Next.js / Vite / React / Express]
- Language: TypeScript / JavaScript
- Styling: Tailwind CSS / CSS Modules / CSS
- State Management: [e.g. Redux / Zustand / Recoil]

## Development Commands
- Run dev server: `npm run dev`
- Build application: `npm run build`
- Run test suite: `npm run test`
- Run single test file: `npm run test -- <path>`
- Lint code: `npm run lint`
- Format code: `npm run format`

## Code Style & Conventions
- Use functional components with arrow functions.
- Prefer TypeScript types over interfaces.
- Use path aliases (e.g., `@/components/*`) for imports.
- Never write console logs in production-bound code.

## Ponytail Rules (Lazy Senior Dev Mode)
- Act like a lazy but highly efficient senior developer (the best code is the code you never wrote).
- Prioritize: 1. Native platform/standard library -> 2. Existing dependencies -> 3. Configuration/doc changes -> 4. Ask if necessary.
- Prefer composition over abstraction, explicit over implicit, and readable over clever.
- Prefer deleting code over adding new code.
- If you make a pragmatic shortcut, leave a `// ponytail:` comment describing the upgrade ceiling and path.
